# keymap_drawer.config.yaml
parse_config:
  raw_binding_map:
    "&td_numpad": "LOWER"
    "&mo RAISE": "RAISE"

draw_config:
  # On utilise la syntaxe dictionnaire simple, sans tirets
  layer_hints:
    lower:
      LOWER: held
    raise:
      RAISE: held
    adjust:
      LOWER: held
      RAISE: held
