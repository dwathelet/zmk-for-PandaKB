parse_config:
  raw_binding_map:
    # On définit le type "mo" pour que l'outil comprenne le lien avec la couche
    "&td_numpad": {t: "LOWER", type: "mo", layer: "lower"}

draw_config:
  dark_mode: true
