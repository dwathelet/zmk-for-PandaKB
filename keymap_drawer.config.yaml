# keymap_drawer.config.yaml
parse_config:
  raw_binding_map:
    "&td_numpad": "LOWER"

draw_config:
  # On force le style "held" (rouge) par index de touche
  # Pour le layer "lower", on cible la touche à la position exacte
  layer_hints:
    "lower":
      - {index: 40, state: held}
