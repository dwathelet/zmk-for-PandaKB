parse_config:
  raw_binding_map:
    # On force l'outil à croire que c'est un Momentary Layer (&mo)
    "&td_numpad": {"t": "LOWER", "type": "mo", "layer": "lower"}
    "&mo RAISE": {"t": "RAISE", "type": "mo", "layer": "raise"}

draw_config:
  dark_mode: true
  # On ne met RIEN dans layer_hints pour laisser l'outil 
  # colorier naturellement ce qu'il croit être des touches de couche.
